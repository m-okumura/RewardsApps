name: poi_app
description: ポイ活アプリ - レシートでポイントを貯める
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # State & Routing
  provider: ^6.1.2
  go_router: ^14.2.0
  # HTTP & API
  http: ^1.2.2
  # Storage
  shared_preferences: ^2.3.2
  # Image picking
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
