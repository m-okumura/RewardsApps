# 本番環境では必ず変更してください
SECRET_KEY=your-secret-key-change-in-production
DEBUG=false

# データベース (Docker の PostgreSQL)
# ポート5432が使用中の場合、docker-composeのportsを5433:5432に変更し、以下を使用
DATABASE_URL=postgresql+asyncpg://poi_app:poi_app_secret@localhost:5433/poi_app

# SQLite を使う場合
# DATABASE_URL=sqlite+aiosqlite:///./poi_app.db
